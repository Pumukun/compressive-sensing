# Прямые зависимости проекта. Транзитивные (contourpy, cycler, fonttools,
# kiwisolver, lazy_loader, networkx, packaging, pyparsing, python-dateutil,
# six, tifffile, imageio) pip разрешает сам - раньше они были перечислены
# здесь с точными пинами, из-за чего установка ломалась на новых Python.
#
# Нижние границы, а не ==: numpy==2.2.3 не имеет колёс под Python 3.14
# и не собирается из исходников.
#
# Проверено на Python 3.14 с numpy 2.5.3, opencv 5.0.0, scikit-image 0.26.0.
# Минимальная версия Python: 3.9.

numpy>=1.24
scipy>=1.10
opencv-python>=4.8
scikit-image>=0.21
matplotlib>=3.7
pillow>=10.0
